 _______                               _
(_______)                _            | |
 _____  _   _ ____ ____ | |_   ____ _ | |
|  ___)| | | / _  )  _ \|  _) / _  ) || |
| |_____\ V ( (/ /| | | | |__( (/ ( (_| |
|_______)\_/ \____)_| |_|\___)\____)____|
                           

May be freely distributed under the MIT license.

To suggest a feature, report a bug, or general discussion:

  http://github.com/andrzejsliwa/evented/issues/

Install required Gems for Building & Testing Process:

  bunlde install

Building:

  $ rake # build

Available tasks:

  $ rake -T
  (in /Users/andrzejsliwa/evented)
  rake build          # rebuild the evented.js file for distribution and specs files for testing
  rake doc            # build the docco documentation
  rake lint           # run JavaScriptLint on the source
  rake spec:autotest  # run specs for evented.js in autotest mode - require run jtestdriver server (rake spec:server)
  rake spec:browser   # run specs for evented.js in browser
  rake spec:connect   # connect browser to jstestdriver server
  rake spec:console   # run specs for evented.js in console - require run jtestdriver server (rake spec:server)
  rake spec:server    # run jstestdriver server
